// Weekly ranking deltas and record streaks are intentionally disabled for the
// initial launch. Re-enable only alongside a projection release that restores
// and validates their materialized tables.
export const RANKING_ENTRY_ENHANCEMENTS_ENABLED = false;
