# Country ranking routes

Country rankings use the `/countries` route group. Each ranking has its own
path and matching API path under `/api/countries`.

The path chooses the statistic. Query parameters only filter the list, such as
`eventId`, `region`, `year`, and `gender`.
