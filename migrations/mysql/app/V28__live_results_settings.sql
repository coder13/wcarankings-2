CREATE TABLE live_results_settings (
  id TINYINT UNSIGNED NOT NULL PRIMARY KEY,
  poll_seconds MEDIUMINT UNSIGNED NOT NULL DEFAULT 3600,
  updated_at DATETIME(6) NOT NULL DEFAULT CURRENT_TIMESTAMP (6) ON UPDATE CURRENT_TIMESTAMP (6)
);

INSERT INTO
  live_results_settings (id)
VALUES
  (1);
