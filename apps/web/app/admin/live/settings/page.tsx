import { LiveResultsSettings } from "@/components/LiveResultsSettings/LiveResultsSettings";

export const dynamic = "force-dynamic";

export default function LiveResultsSettingsPage() {
  return <LiveResultsSettings />;
}
